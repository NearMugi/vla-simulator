# Step 5: 学習データ収集機能 (Training Mode) 実装計画

## 目的
人間が手動操作した際の「画像」と「アクション（座標・グリッパー）」をペアで保存し、Octo モデルの Fine-tuning に使用可能なデータセットを作成する。

---

## タスク一覧

### 1. フロントエンド: データ収集 UI の実装
*   **録画コントロール**: 
    *   「REC START」/「REC STOP」ボタンを UI に追加。
    *   録画中のステータス表示（例：赤い点滅インジケータと「Recording episode #001...」の表示）。
*   **エピソード情報**:
    *   現在の命令（Instruction）が保存データに含まれるように紐付け。
    *   成功/失敗のフラグを任意で設定できる仕組み（まずは全て成功データとして保存する簡略版から開始）。

### 2. フロントエンド: データ送信ロジックの実装
*   **定期キャプチャ**: 
    *   録画開始時、一定間隔（例：200ms〜500ms）で現在のキャンバス画像と座標データを取得。
    *   推論時と同様のフォーマットでバックエンドの新規エンドポイントへ送信。
*   **エピソード管理**:
    *   録画開始ごとに一意の `episode_id`（タイムスタンプ等）を生成。

### 3. バックエンド: データ保存 API (`/save_step`) の作成
*   **エンドポイント追加**: 
    *   `POST /save_step`: 画像ファイルと姿勢データ（Form-data）を受信。
*   **ストレージ構造**:
    *   `datasets/<episode_id>/` ディレクトリを作成。
    *   画像（`step_000.png` ...）と、それに対応するアクション情報を記録したメタデータ（`episode_metadata.json` または各ステップの JSON）を保存。

### 4. 改善要素（Step 5 の後半で実施）
*   **データ分割**: 保存時に `train` / `val` のフォルダに自動振り分け。
*   **動作確認**: 保存された画像をパラパラ漫画のように再生して、正しく記録されているか確認する簡易ビューア（またはログ確認）。

---

## 作業の進め方
1.  **Backend**: `datasets/` ディレクトリの準備と `/save_step` エンドポイントの実装。
2.  **Frontend**: 録画ボタンの設置と、定期的なデータ送信処理の実装。
3.  **検証**: 実際に操作して `datasets/` 以下にファイルが生成されることを確認。

---

まず、**バックエンドの保存用APIの実装**から着手してよろしいでしょうか？
